namespace Ca.SharedKernel
{
    /// <summary>
    /// Represents aggregate root entities, Repositories will only work with aggregate roots, not their children
    /// </summary>
    public interface IAggregateRoot
    {
    }
}